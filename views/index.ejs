<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ShortDesk</title>

  <!-- ✅ Favicon & PWA Icons -->
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="manifest" href="/site.webmanifest">
  <meta name="theme-color" content="#81c784">

  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="light">
  <main class="glass home-page">
    <h1 class="brand">ShortDesk ✨</h1>
    <p class="tagline">Turn long messy URLs into beautiful smart links</p>

    <% if (error) { %>
      <div class="alert"><%= error %></div>
    <% } %>

    <form class="form" method="POST" action="/shorten">
      <div class="form-group">
        <label for="originalUrl">Paste your long URL</label>
        <input id="originalUrl" name="originalUrl" type="url" placeholder="https://example.com/very/long/link" required />
      </div>

      <div class="form-group">
        <label for="customAlias">Custom alias (optional)</label>
        <input id="customAlias" name="customAlias" type="text" placeholder="e.g. myproject" />
      </div>

      <button type="submit" class="btn-main">✨ Create Link</button>
    </form>

    <footer>Made with ❤️ by Nilay</footer>
  </main>
</body>
</html>